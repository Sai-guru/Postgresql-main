// prisma/schema.prisma
// Use the DATABASE_URL from your .env which points to Neon
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Example model — adjust to match your existing table schema or run `prisma db pull` to introspect
model User {
  id        Int     @id @default(autoincrement())
  name      String?
  email     String  @unique
  is_active Boolean @default(true)
}
