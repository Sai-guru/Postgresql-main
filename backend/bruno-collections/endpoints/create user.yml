info:
  name: create user
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/api/new
  body:
    type: json
    data: |-
      {
        "name": "jamudfsfd","email":"jam@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
