info:
  name: getAllUsers
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/api
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
