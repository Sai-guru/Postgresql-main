info:
  name: getUsers by id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4000/api/22
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
