### Example environment variables for Neon (do NOT commit your real secrets)
# Replace the placeholder values with your Neon connection details.
# For Prisma migrations with pooled/unpooled settings see README.
#
# Example pooled connection (recommended for runtime):
DATABASE_URL="postgresql://<NEON_USER>:<NEON_PASSWORD>@<NEON_HOST>/<NEON_DB>?sslmode=require&channel_binding=require"

# Optional: unpooled direct connection used by Prisma migrate or introspection in some Prisma versions
# Uncomment and fill if you need an unpooled connection when running `prisma migrate` locally.
# DATABASE_URL_UNPOOLED="postgresql://<NEON_USER>:<NEON_PASSWORD>@<NEON_HOST>/<NEON_DB>?sslmode=require&channel_binding=require"

# Server port (optional)
PORT=4000
