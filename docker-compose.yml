version: "3.9"

services:

  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    expose:
      - "4000"

  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend